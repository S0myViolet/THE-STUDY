import type { FirstMonthWeek } from "@/lib/v2/content-types";

/** Default first thirty days for a balanced learner; the planner adapts it to the baseline. */
export const FIRST_MONTH: FirstMonthWeek[] = [];
